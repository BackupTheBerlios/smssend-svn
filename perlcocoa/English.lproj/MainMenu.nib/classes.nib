{
    IBClasses = (
        {
            ACTIONS = {invia = id; }; 
            CLASS = "Ctrl_invio"; 
            LANGUAGE = ObjC; 
            OUTLETS = {messEdit = id; numberEdit = id; prefixEdit = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}